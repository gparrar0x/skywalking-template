'use client'

/**
 * Browser Supabase client — use in Client Components only.
 * Typed with your project's Database type once generated.
 *
 * @example
 * import { createClient } from '@/lib/supabase/client'
 * const supabase = createClient()
 */

// Replace `Database` with your generated Supabase types when available.
// import type { Database } from '@/lib/supabase/database.types'

export { createClient } from '@skywalking/core/supabase/client'
